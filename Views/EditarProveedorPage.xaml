<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartMenu.Views.EditarProveedorPage"
                          BackgroundColor="#FFE066"  
             Title="Editar Proveedor">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label Text="Editar proveedor" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="Black"/>
            <Entry Text="{Binding Nombre}" Placeholder="Nombre"   BackgroundColor="White" TextColor="Black"/>
            <Entry Text="{Binding Contacto}" Placeholder="Contacto"  BackgroundColor="White" TextColor="Black"/>
            <Entry Text="{Binding Telefono}" Placeholder="Teléfono" Keyboard="Telephone"  BackgroundColor="White" TextColor="Black"/>
            <Entry Text="{Binding Correo}" Placeholder="Correo" Keyboard="Email"  BackgroundColor="White" TextColor="Black"/>
            <Button Text="Guardar cambios"
                    Command="{Binding GuardarCommand}"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    CornerRadius="15"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>