<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartMenu.Views.Comida"
             BackgroundColor="#FFE066"
             Title="Comida">

    <ContentPage.ToolbarItems>
        <ToolbarItem
            IconImageSource="carrito.png"
            Text="Carrito"
            Order="Primary"
            Priority="0"
            Clicked="AbrirCarrito_Clicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="25" HorizontalOptions="Center">

            <Label
                Text="Comida"
                FontSize="32"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="Black"/>

            <CollectionView x:Name="PlatillosCollection" ItemsLayout="VerticalList" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout Spacing="5" HorizontalOptions="Center">

                            <Border StrokeThickness="0" BackgroundColor="Transparent" StrokeShape="RoundRectangle 20">
                                <Image Source="{Binding Imagen}"
                                       WidthRequest="150"
                                       HeightRequest="150"
                                       Aspect="AspectFill" />
                            </Border>

                            <Label Text="{Binding Nombre}" FontSize="18" TextColor="Black" HorizontalOptions="Center" />
                            <Label Text="{Binding Precio, StringFormat='${0:F2}'}" FontSize="14" TextColor="DarkGreen" HorizontalOptions="Center" />

                            <Button Text="Agregar al carrito"
                                    BackgroundColor="#DC3545"
                                    TextColor="White"
                                    CornerRadius="20"
                                    WidthRequest="180"
                                    CommandParameter="{Binding .}"
                                    Clicked="AgregarAlCarrito_Clicked" />
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

